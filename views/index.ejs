<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Express js</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <% if(status == "yes"){ %>
              <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
              </li>
              <% }else{ %>

                <li class="nav-item">
                  <a class="nav-link" href="/login">Login</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="/register">Register</a>
                </li>
                <% } %>
            </ul>
            
          </div>
        </div>
      </nav>

      <div class="container my-5">
        <div class="card" style="width: 100%;">
          <div class="card-body">
            <% if(status == "yes"){ %>
            <h5 class="card-title">Welcome <%= user.email %></h5>
            <% }else{ %>
              <h5 class="card-title">Register your account</h5>
              <% } %>
            <!-- <h6 class="card-subtitle mb-2 text-muted">Card subtitle</h6> -->
            <p class="card-text">This is login/registration module in express js and mysql</p>
            <p class="card-text">Packages used in : npm i bcryptjs,cookie-parser,dotenv ,ejs ,express ,jsonwebtoken ,mysql ,nodemon</p>
            <p class="card-text">'dotenv' package for environment variable used by process.enc.ENV_NAME</p>
            <% if(status == "yes"){ %>
            <a href="/" class="card-link">Home</a>
            <a href="/logout" class="card-link">logout</a>
            <% }else{ %>
              <a href="/login" class="card-link">Login</a>
            <a href="/register" class="card-link">Register</a>
              <% } %>
          </div>
        </div>
      </div>
    
</body>
</html>